/**
 * User-facing product name for the operator schedule assistant (floating chat, schedule settings, toasts).
 * Internal code, API paths, and DB identifiers stay as operator/schedule AI.
 */
export const OPERATOR_SCHEDULE_AI_DISPLAY_NAME = 'Jarvis'
