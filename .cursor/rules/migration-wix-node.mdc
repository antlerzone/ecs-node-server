---
description: 架构约定：Next.js + Node + MySQL，数据在 MySQL，文件 OSS
alwaysApply: true
---

# 架构约定

- **当前架构：** **前端 Next.js**（Portal / Operator / SaaS Admin）/ **后端 Node（ECS）** / **数据库 MySQL**。业务数据全部在 MySQL。
- **图片 / 文件上传：** 使用阿里云 OSS，表里存 URL。

## 开发时必须遵守

1. **所有业务数据** 从 MySQL 读写；后端只写 Node + MySQL。
2. **表/列不确定时** → 先与维护者确认，再写或改代码。

外键约定（一律用 `_id`）见同目录 `mysql-fk-use-id-only.mdc`。  
文档详见 `docs/index.md`。
