---
description: CNYIoT 电表 — 仅平台母账号体系、Operator 不自接外站；加表与租客 station 术语（改 meter/cnyiot 相关代码时必查）
alwaysApply: true
---

# CNYIoT（智能电表）— SaaS 平台模型

## 产品事实（Operator / Company）

- Coliving Operator 在 **Company → CNY IoT** 使用 **平台电表账号**；**不能**让 Operator 绑定自己在 CNYIoT 官网另注册的独立主账号。连接即 **平台代创**（无凭证连接文案见 `coliving/next-app/app/operator/company/page.tsx`）。
- 后端 `cnyiotConnect` 仅允许平台创建流程；非 `create` 会 `CNYIOT_PLATFORM_ONLY`（见 `src/modules/companysetting/companysetting.service.js`）。

## 对维护者的一句话

**对 Operator 而言没有「第二套自建 CNY 主账号」。** CNYIoT 侧是：**env 母账号（`CNYIOT_LOGIN_NAME`）+ 平台为每家公司创建的租客（station_index）**；代码里常叫 `cnyiot_subuser`，**不是**「与母账号无关的运营商自有外站账号」。

## 代码约定

1. **`callCnyIot`（[`cnyiotRequest.js`](src/modules/cnyiot/wrappers/cnyiotRequest.js)）默认 `usePlatformAccount: true`**：所有 CNYIoT HTTP 调用一律用 **env 母账号**（`CNYIOT_LOGIN_NAME` / `CNYIOT_LOGIN_PSW`），除非显式传入 `usePlatformAccount: false`（**禁止**在新功能中使用；仅保留供极端排障）。
2. **加表**：`insertMeters` → `addMeter` / `link2User` 在母账号下执行，再把表挂到该公司的 **station_index**（`UserID`）。勿改成「仅用租客 token 加表」。
3. **术语**：`cnyiot_subuser_id` / `ensureClientCnyiotSubuser` = 平台在母账号下创建的 **CNY 租客**，用于分组与 link；本地仍存 `cnyiot_subuser_login` 等供审计/历史，**不是**让 API 默认改走租客登录。

## 参考文件

- `src/modules/cnyiot/wrappers/meter.wrapper.js`（`addMeters`、`setRelay`）
- `src/modules/cnyiot/wrappers/cnyiotRequest.js`（`usePlatformAccount`）
- `src/modules/cnyiot/lib/cnyiotToken.service.js`（母账号 vs 按 client 登录）
- `src/modules/cnyiot/lib/cnyiotSubuser.js`
