---
description: JSW 中 postJson 返回值必须用 JSDoc 断言类型，避免 TypeScript 报 unknown / Property 'ok' does not exist
alwaysApply: true
---

# JSW 类型约定（Velo backend .jsw）

## 问题

Wix Velo 的 JSW 里，`await postJson(...)` 或 `await res.json()` 的返回值在 TypeScript/IDE 中会被推断为 **`unknown`**，导致：

- `Property 'ok' does not exist on type 'unknown'`
- `Type 'unknown' is not assignable to type '{ options: ... } | { ok: false, reason: string }'`

## 必须遵守

1. **`postJson` 内部**  
   - `return data` 改为：`return /** @type {Object} */ (data);`，避免返回 `unknown`。  
   - 错误分支里 `const errBody = await res.json();` 改为：`const errBody = /** @type {{ reason?: string } | null } */ (await res.json());`，避免 "Property 'reason' does not exist on type 'unknown'"。

2. **每个调用 `postJson` 的导出函数**  
   对 `const data = await postJson(...)` 的 **`data` 做一次 JSDoc 类型断言**，类型要包含：
   - 成功时的字段（如 `options`、`items`、`ok`、`inserted` 等）
   - 失败时的字段：`ok?: boolean`、`reason?: string`
   - 末尾加 `| null`（因为失败分支可能返回 null）

   示例：

   ```js
   // 返回 options 数组的接口
   const data = /** @type {{ options?: Array<{label:string, value:string}>, ok?: boolean, reason?: string } | null */ (await postJson('/api/xxx/suppliers', {}));
   if (data && data.ok === false) return data;
   return data || { options: [] };
   ```

   ```js
   // 返回 items + 分页
   const data = /** @type {{ items?: any[], totalPages?: number, total?: number, ok?: boolean, reason?: string } | null */ (await postJson('/api/xxx/list', opts));
   if (data && data.ok === false) return data;
   return data || { items: [], totalPages: 1, total: 0 };
   ```

   ```js
   // 只返回 ok
   const data = /** @type {{ ok?: boolean, reason?: string } | null */ (await postJson('/api/xxx/save', payload));
   if (data && data.ok === false) return data;
   return data || { ok: true };
   ```

3. **@returns 用宽松类型**  
   导出函数的 JSDoc 用 **`@returns {Promise<*>}`**，不要写具体的 `{ items: Array<{_id:string}> } | { ok: false }` 等，否则与实际 `return data || { items: [] }` 容易产生类型不匹配红线。保留 `@param` 即可。

4. **不要** 在 JSW 里写 `$w('#xxx').offClick()`；Wix Button 没有 `offClick`，要用 **`$w('#xxx').onClick(null)`** 解绑。

## 参考

- 已按上述方式改好的示例：`docs/wix/jsw/velo-backend-saas-propertysetting.jsw.snippet.js`、`docs/wix/jsw/velo-backend-saas-ownersetting.jsw.snippet.js`
- 之后新增或修改任何 `docs/wix/jsw/*.jsw.snippet.js` 或 Wix 后台的 `backend/saas/*.jsw` 时，**一律**对 `await postJson(...)` 的接收变量做 `/** @type { ... } */` 断言，避免「整个后端都是红线」（Property 'ok' does not exist on type 'unknown'、Type 'unknown' is not assignable to type '...'）。
