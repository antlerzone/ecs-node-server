---
description: Import 时凡有 wix_id/*_wixid 必须解析为 _id (FK)，并同步 Junction
alwaysApply: false
globs: scripts/import-*.js
---

# Import 脚本：id 与 FK（0087 后）

## 必须遵守

1. **Import 时 id：** CSV 的 `_id`（Wix 主键）直接写入 `id`，**不生成新 UUID**。只有 API/UI 新建 item 才用 `randomUUID()`。

2. **CSV reference 列：** 直接写入对应 `_id` 列（client→client_id、property→property_id 等），无需查表。见 `docs/db/import-wixid-to-fk-junction-rule.md`。

3. **若 FK 参与 Junction 表，必须在同一次 import 里写入 Junction。**
   - 例：import **account** 时若写了 `client_id`，必须同时 `INSERT` **account_client**。
   - 例：import **ownerdetail** 时若写了 `client_id` / `property_id`，必须同步 **owner_client** / **owner_property**。

## 参考

- 列 ↔ FK ↔ 查表 ↔ Junction 完整表：`docs/db/import-wixid-to-fk-junction-rule.md`
- 所有 FK 与 Junction 表：`docs/db/fk-and-junction-tables.md`
