---
description: 改 UI 或后端后，任务结束前必须 build（若动 UI）并重启 Node，便于浏览器/API 立刻生效
alwaysApply: true
---

# 改 UI / 后端：结束前必须 build（视情况）+ 重启 Node

用户与 agent 协作时，只要任务里**改了代码**，**在结束任务之前**必须在项目根目录 `/home/ecs-user/app` 自行做完下面事项，**不要**只改源码就收尾，也不要让用户自己去重启（除非终端无法执行命令）。

## 改了 UI（Next.js：`docs/nextjs-migration/**` 等）

1. **`npm run build:portal`** — 产出最新前端构建，否则浏览器仍可能是旧资源。
2. **重启提供服务的 Next 进程**（如 `portal-next`）— `next start` 在启动时登记静态资源；**仅 build 不重启**可导致 `/_next/static/chunks/*.js` **404**（即使磁盘上已有文件）。

**目的：** 用户 **F5 刷新** 即可看到新界面。

## 改了 Cleanlemons Portal（Next.js：`cleanlemon/next-app/**`）

1. **`npm run build:cleanlemons-portal`**（或 `cd cleanlemon/next-app && npm run build`）— 生产构建已固定为 **webpack**（与 Coliving 一致），勿改回默认 Turbopack `next build`，以免 chunk / 静态资源异常。
2. **重启所有**提供该目录 `.next` 的 PM2 进程 — `next start` **只在启动时**登记 `.next/static` 下的文件；**build 后不重启**时，磁盘上已有新 `webpack-*.js` 等仍可能 **404**。若 ECS 上同时有 `next-cleanlemons` 与 `next-cleanlemons-3000`，**必须一并重启**。
3. **推荐一键部署**：**`./scripts/deploy-cleanlemons-portal.sh`**（会 `rm -rf .next`、build、重启）。  
4. **ECS 自动防漏**：`cleanlemon/next-app` 的 `npm run build` / `build:low` 在成功后可选 **自动 `pm2 restart`**（标记文件 **`.enable-pm2-restart-after-build`** 或 **`RESTART_PM2_AFTER_NEXT_BUILD=1`**），见 `cleanlemon/docs/portal-chunk-prevention.md`。

详见 `cleanlemon/docs/portal-chunk-prevention.md`。

## 改了后端（`src/**`、`server.js`、API 等）

1. **必须重启 Node**，让新逻辑生效（与改 UI 一样，由 agent 在终端处理）。
2. 根目录可执行 **`npm run build`**（若有真实构建会生效；当前多为占位可照常跑）。

**目的：** 接口与业务行为立即按新代码运行；不要假设「保存了就会自动重载」就跳过——若当前**没有** nodemon 自动重载，或不确定是否已重载，**应主动重启**（先停再起 `npm run dev` / `npm start`，或按实际部署方式重启）。

## 同时改 UI 与后端

- 顺序：**先** `npm run build:portal`（Coliving）或 **`npm run build:cleanlemons-portal`**（Cleanlemons），**再** `npm run build`（若需要），**最后**重启对应 PM2 / Node 进程。

## 生产环境

- 若进程由 systemd/pm2 等管理，以实际重启命令为准；避免未确认的 `pkill` 误杀无关进程。
