---
description: MySQL 外键统一用 _id；0087 后 id = Wix _id，已无 _wixid 列
alwaysApply: true
---

# 外键命名与使用约定（Wix reference → MySQL）

- **0087 后：** 所有表 `id` = Wix CMS 的 `_id`（UUID 同格式）。已删除 `wix_id`、`*_wixid` 列。
- **FK：** 一律用 `_id` 列（如 `property_id`、`client_id`），指向主表 `id`。

## Node 开发必须遵守

- **一律用 `_id` 做主关联。** 查询、JOIN、写入 FK 时只用 `client_id`、`property_id` 等。
- **Import：** Wix 导出的 `_id` 直接写入 `id`；reference 列直接写入对应 `_id`。

文档见 `docs/index.md`「Wix reference → id 约定」与 `docs/db/import-wixid-to-fk-junction-rule.md`。
